<form method="post" action="{{ path('app_diem_delete', {'id': diem.id}) }}" onsubmit="return confirm('Bạn Chắc Chắn Muốn Xóa Nó Chứ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diem.id) }}">
    <button class="btn btn-danger">Xóa</button>
</form>
