<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Quản Lý Học Sinh{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div class="container-fluid">
            <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            {% if app.user %}
              <a href="/homepage" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
            {% else %}
              <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
            {% endif %}
                <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
                <span class="fs-4">Quản Lý Học Sinh</span>
              </a>
            {% if app.user %}
              <ul class="nav nav-pills" style="margin-left: 10px;">
                <li class="nav-item"><a href="{{path('app_home_page')}}" class="nav-link active" style="margin-left: 5px;">Trang Chủ</a></li>
                <li class="nav-item"><a href="{{path('app_hoc_sinh_index')}}" class="nav-link active" style="margin-left: 5px;">Quản Lý Học Sinh</a></li>
                <li class="nav-item"><a href="{{path('app_lop_index')}}" class="nav-link active" style="margin-left: 5px;">Quản Lý Lớp</a></li>
                <li class="nav-item"><a href="{{path('app_mon_hoc_index')}}" class="nav-link active" style="margin-left: 5px;">Quản Lý Môn Học</a></li>
                <li class="nav-item"><a href="{{path('app_diem_index')}}" class="nav-link active" style="margin-left: 5px;">Quản Lý Điểm</a></li>
                <li class="nav-item"><a href="{{path('app_hoc_ky_index')}}" class="nav-link active" style="margin-left: 5px;">Quản Lý Học Kỳ</a></li>
                <li class="nav-item"><a href="{{path('app_lich_hoc_index')}}" class="nav-link active" style="margin-left: 5px;">Quản Lý Lịch Học</a></li>
              </ul>
            {% endif %}
            </header>
            
        {% block body %}{% endblock %}
        <footer class="py-3 my-4">

            <ul class="nav justify-content-center border-bottom pb-3 mb-3">
                {% if app.user %}
                <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link px-2 text-body-secondary">Đăng Xuất</a></li>
                {% endif %}
              <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Home</a></li>
              <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Features</a></li>
              <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Pricing</a></li>
              <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">FAQs</a></li>
              <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">About</a></li>
            </ul>
            </footer>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    </body>
</html>
