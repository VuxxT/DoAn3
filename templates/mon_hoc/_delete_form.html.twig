<form method="post" action="{{ path('app_mon_hoc_delete', {'id': mon_hoc.id}) }}" onsubmit="return confirm('Bạn Chắc Chắn Muốn Xóa Nó Chứ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mon_hoc.id) }}">
    <button class="btn btn-danger">Xóa</button>
</form>
